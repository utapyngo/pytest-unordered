os: linux
language: python
python:
    - "3.5"
    - "3.6"
    - "3.7"
    - "3.8"
    - "pypy3"
install:
    - pip install tox-travis
    - pip install coveralls
script: tox
env:
    - COVERALLS_SERVICE_NAME=travis-ci POST_COMMAND=coveralls
